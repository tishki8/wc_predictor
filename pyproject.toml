[project]
name = "wc-predictor"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "flask",
    "flask-cors",
    "pandas",
    "numpy",
    "statsmodels",
    "scipy",
]

[tool.vercel]
entrypoint = "app.server:app"